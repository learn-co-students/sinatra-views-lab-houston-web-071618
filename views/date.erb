<!DOCTYPE html>
<html>
    <head>
    </head>
    <body>
      <% date = Time.new %>
      <h1> <%= "The date is  " + date.strftime("%A") + ", " + date.strftime("%B") + " " + date.strftime("%d") + ", " + date.strftime("%Y") %></h1>
    </body>
</html>
